---
# Fast redeploy: pull latest code and rebuild without reinstalling system deps
- name: Redeploy TerribleSoft application
  hosts: terriblesoft
  become: true
  vars:
    force_build: true
  roles:
    - role: ../roles/app
